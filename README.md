# COFFEE MACHINE WATER LEVEL MEASUREMENT SYSTEM (CWLS)

## Flash microcontroller

Hold Botsel Buton, THEN insert USB Cable. Run the command:

```
cargo run  --release --example pico_usb_twitchy_mouse
```